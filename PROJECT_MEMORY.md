# Project memory

## What changed

2026-09-11：以 `4208ebd` 为基线，补课程落盘承诺、冲突409与确认条件。后续已补本机/云端选择、事务内双方备份、IDB请求日志与幂等回执；修首批误入全量分支、复习按批详情、统计50条分页、内容版本绑定游标，新增8000句模拟容量测试。当前状态以 architecture-plan.md §9～§11 为准。

## What was learned

“调用异步存储”不等于“数据已保存”；“SQL 没抛错”不等于“版本写入被接受”。修复必须贯穿保存结果、用户提示、重试水位和刷新恢复，不能只改变按钮文案。串行化是单页面保证，不代表多标签页或多设备并发已被解决。

## Verification

课程迁移/写入失败和真实课程导入通过专项测试；API 错误元数据与客户端回执通过单测；服务端四类实体冲突和事务回滚通过 smoke；双浏览器验证同版本冲突及本地值保留；真实 IDB 验证升级与刷新数据保留。最终套件结果见 PROJECT_PROGRESS.md。

## Next steps / open questions

后端普通同步已新增可选baseRevs（§12）；前端确认基线与持久待发日志仍待实施，当前普通客户端尚未启用条件写。仅包装网络请求不足以保护“保存后立即关闭”的窗口，必须覆盖保存入口。冲突处理界面与其请求恢复已完成，不等同于全局并发控制。整库恢复跨存储原子性、多标签页旧快照、行级删除恢复与账号隔离仍需收敛。8000句模拟通过不等于真机验收；当前复习交接仍是localStorage轻量引用队列，需要迁入IDB。保持历史cid，按业务课程组织内容，而非把物理分片直接当课程。
