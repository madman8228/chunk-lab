# Project progress

## Current status

2026-09-12 最新收口审计：修复刷新后同步代次未恢复、连续快速保存复用同一代次的两个数据一致性边界；并修复恢复 cloud-hold 错误拦截受控冲突比较/解决接口的问题，恢复后可核对云端并在确认后解除暂停；重复恢复使用新的请求编号；旧页面收到 428 会显示刷新提示。题库发布已纳入持久化条件队列，丢回执后可原样重试。部署脚本要求显式目标主机，新增本地全套（主套件、账号、批次、8000句移动）回归和远端生产配置 fail-closed 预检；公网安全冒烟新增未认证 API 拒绝检查。批次真实页面 22/0、修订同步 53/0、`npm test` 退出码 0。迁移专项改为系统临时端口后连续两次 9/0；跨平台 LF/CRLF 哈希回归、多标签页专项 12/12 通过；Service Worker 当前为 `chunklab-8602257a`。自动代码侧本轮已按主题提交，尚未部署。

上线前仍必须完成的不是继续改代码，而是外部验收：生产鉴权/HTTPS/限流与密钥、独立实例备份恢复演练、Android/iOS 真机主流程、旧页面升级后的关闭旧页操作和发布授权。完整统一权威事务、8000 句内容生产/智能合并属于后续工作，不作为首版小批量发布门槛。

2026-09-12 最新同步收口：整账号选择产生的事件墓碑已接入全量/增量下行，其他设备拉取后不会复活；事件 ID 回执匹配改为按不可变 ID，避免旧 cid key 迁移使待发日志卡住；在途同步请求会等待并发送最新代次。`node e2e/sync.test.js` 24/0，`npm test` 退出码 0，Service Worker 为 `chunklab-b8968286`。自动回归已覆盖保存、迁移、冲突、删除/重建和 8000 句模拟；尚未部署。

当前真正的上线门槛只剩外部验收与少量兼容边界：生产鉴权/HTTPS/限流、独立实例备份恢复演练、Android/iOS 真机主流程、旧页面升级提示后的关闭旧页操作和发布授权。完整业务统一权威存储、智能合并、全量增量下行和 8000 句内容生产可延期，不应继续扩大本轮修复范围。

2026-09-12 最终自动收口：慢 IndexedDB 下统计写入改为最多保留当前提交和一个最新尾提交，避免 8000 句答题产生无限 Promise/写入积压；移动 8000 句专项 18/0（含 40ms 慢写入，最多2个提交），`npm test`、`test:accounts`、`test:batch-sync`、stats-IDB 22/0 均通过。Service Worker 当前为 `chunklab-6c4b054b`。自动代码实施暂不再扩展；生产配置、隔离恢复、真机和发布授权仍未完成，未部署。

2026-09-12 代次边界补强：课程/进度保存与 `conditional-batch-v1.localGeneration` 在同一 IDB 事务内推进；课程作用域统一复用 AccountStorage 规范 owner，兼容旧页面直接写 token。真实批次同步回归 19/0，最终 Service Worker 为 `chunklab-34bc9941`；未部署。

2026-09-12 继续执行：8000句热路径移除每次保存对整份 stats 的深拷贝，保留 IDB 事务内的变更行结构化克隆；主页面新建/追加题库、题库页公共/内置题库导入改为等待本地提交 Promise 后再刷新或跳转，保存失败留在当前入口并提示。专项 stats-IDB 21项、8000句移动端模拟15项、npm test 通过；SW 已重生。R1 原子 business-mem/generation、GET 一致快照、B/C 冲突恢复和真机/生产门槛仍未完成，未部署。

2026-09-12 继续：服务端 `/api/data` 与导出读取使用 SQLite 单事务快照，保证返回数据与 `seq` 同时代；普通条件同步入队增加本地 `expectedGeneration` 校验，本地编辑抢先发生时拒绝旧 payload 并安排当前数据重建。服务端下游回归59项、批次同步回归17项通过；8000句模拟补充连续100次保存，p50约10ms、p95约13ms、最大约16ms，最终8000条句档案与100条事件均保留；最终 `npm test` 通过，SW 当前为 `chunklab-5d6f0c2b`。A包仍未完成，未部署。

上线D1推进：修复API旧会话回执穿透与晚到401清新token，见 RELEASE_CHECKLIST.md D1执行记录。存储隔离未完成，D1继续保持未通过；未增加新工作包，未部署。

上线收口入口改为 RELEASE_CHECKLIST.md：冻结新增功能，以5道发布门和D1—D4四个数据安全工作包为准。旧段落是过程记录，不再自动扩大待办。2026-09-12开始D1账户隔离优先验收；尚未放行生产。

2026-09-12 学习删除恢复：启动拉取等待学习写队列，读取本账号日志，合并后再次应用待发 sentenceStats 墓碑并标记待同步；上行从持久墓碑补充 sbsGone，不依赖刷新已丢失的云端签名。成功回执按操作ID清理。真实接口离线删除→刷新→云端删除→日志清理通过，Playwright共34项、npm test通过。仅按现有无行版本删除协议实现，未解决另一设备同时重建同一档案的冲突；事件删除、非删除日志完整重放、已合并值与旧日志对账清理、快照积压性能均仍待完成。没有生产部署。

2026-09-12 学习上传回执：普通发送等待 stats 写入队列，读取当前账号学习日志，按实际 statsDelta 的行内容、事件内容或删除 key 匹配回执；明确成功后使用原子 operationId 比较删除，保留在途新修改。Playwright 回归33项通过，新增成功清理和旧回执保留新日志。尚未新增刷新后的学习重放，删除仍需持久日志恢复接线；快照积压、真实8000条连续保存性能测试仍待完成。测试中的学习上传使用模拟成功回执，不能替代跨设备学习冲突验证。

2026-09-12 学习日志接线：writeStatsBatch 可在同一事务写 sentenceStats/events 的逐条 syncIntents，常规 persistStats 已传入账号分组；显式删除留下墓碑日志，日志失败回滚业务数据。云端合并保存不新增学习日志。readLearningIntents 与课程读取分离，课程恢复不消费学习记录。浏览器回归31项覆盖日志故障回滚及刷新删除保留。学习日志尚无自动消费/回执清理，旧上传逻辑仍工作；事件整体替换未定义远端删除协议，全量重建未补齐删除差异。快照积压优化与连续8000条答题热路径测试尚未完成。上述限制须在启用学习重放前解决，不能宣称全链路完成。

2026-09-12 学习记录准备阶段：新增 IDB writeStatsBatch，将正常保存中的句子档案增删与事件追加/替换合为单事务；core 按快照串行执行学习记录保存，waitForSync 包含该写入队列。尚未接学习记录的持久上行日志，不能据此宣称学习记录刷新自动重放完成。启动迁移仍需进一步统一事务。快照当前复制 stats，需单独测8000条连续答题保存的耗时和峰值；现有容量测试不覆盖该热路径。

本轮验证：npm test 通过；Playwright 存储29项通过（新增事件写失败时档案回滚）；8000句模拟15项通过，首页625ms、统计678ms、到期渲染145ms、复习664ms、采样堆20MiB。没有真机验收或部署。

2026-09-12 最新：课程/课程进度的持久日志已接入自动恢复。启动拉取前、普通发送前先排空当前账号日志；核对原内容与远端内容一致后使用远端实际 rev 条件写，内容不同进入冲突流程。请求在 IDB 冻结后才发送，后继编辑保留冻结请求；回执丢失可原样重试，成功后事务确认/推进后继。拉取写回保护期间新日志，显式冲突选择按操作编号清理。服务端实体读取新增 exists 以区分缺失与墓碑。真实浏览器回归28项通过；全量测试通过。以下日期段为过程记录，以本段为准。

边界：仅覆盖经 writeCourses/writeProgress 保存的课程/进度；题库、小字段、统计行与事件仍未接同等队列。首次基础版本来自“原内容一致”的保守对账，不是历史远端 rev 全覆盖（无法识别改回相同内容的 ABA）。未登录 local 日志不会自动转给登录账号。旧协议仍兼容。独立 js/sync-outbox.js 保留为未接线实验模块，实际恢复复用业务 IDB syncIntents，避免跨库假原子性。未部署。

2026-09-11：完成数据可靠性第一阶段、明确冲突处理入口，以及8000句按批复习/统计分页和模拟容量门槛。全项目路线与未完成边界见 architecture-plan.md §0、§9～§11。

## Verification

- 新增课程存储与 API 回执测试，已纳入 `npm test`。
- `npm test` 前后端全量测试通过（含新增异常回归）。
- 真实浏览器：双设备同步/选择/备份/刷新22项、增量同步35项、IDB迁移/刷新20项、分批6项、轻量索引6项、8000句模拟容量15项通过，共104项。
- 容量代表性复跑（393×852，CPU×4）：首页589ms、统计639ms、8000条到期渲染141ms、复习跳转634ms；采样堆约20MiB，非全程峰值。首页零详情请求，跨片两组只取2个详情分片。
- 部署依赖检查和 Service Worker 版本一致性检查通过。
- 未部署生产、未改动用户课程数据；不宣称已经完成 8000 句真实移动设备验收。

## Next concrete work item

2026-09-12：新增独立 `js/sync-outbox.js`（尚未接保存入口/HTML）。按服务地址和账号隔离、逐实体 IDB 事务保存；queued 合并草稿、sending 请求不可变并保留后继改动；确认回执才推进基线；未知基线/冲突暂停发送。`sync-outbox.test.js` 通过；真实浏览器 `e2e/sync-outbox.test.js` 7项通过（刷新、双标签页、账号隔离、整批回滚）。这不是“业务保存和待发意图已经原子提交”，也未提供冲突解决后的队列重建或行级实体队列。

用户已确认由本任务统一推进，保留现有多标签页合并。补齐保存失败不广播/不调度同步，以及 IDB 全量替换发生同步异常时显式中止事务，避免 clear/部分 put 意外提交。新增存储失败回归与真实 IDB 回滚验证。

2026-09-12 续：课程和课程进度的实际保存入口改用 IDB 单事务读取/三路合并/写入，不再依赖跨标签页通知及时送达；启动课程迁移也在同一事务读取最新值后合并。旧存储适配器保留兼容路径，该路径不具备新事务保证。待发模块仍未与业务事务、上传回执接通，不宣称离线队列端到端完成。

后端普通上行已支持可选 baseRevs 条件写（§12），四类实体写入/删除与参数校验已覆盖，后端 smoke 178项通过。前端尚未接入：下一步先梳理全部保存入口，再实现持久基线与 outbox、启动恢复及冲突衔接；不能只在网络调用处缓存请求。随后迁移复习交接至IDB并做真机验收。没有生产部署，没有创建提交。

2026-09-12 保存入口接线：IDB 升到 v3，新增 syncIntents。实际 writeCourses/writeProgress 在读取/合并/业务写入的同一事务中保存逐实体意图（包含删除、操作 ID、首次本地原值与最新值）；按服务地址与账号分组，未登录记录使用 local 分组，不猜测开放模式身份。排队期间账号切换会拒绝保存。任何日志写入失败均回滚业务写入。迁移/云端写入 sync=false 不新增意图。旧适配器不具备该保证。

验证：Playwright 存储回归16项、IDB迁移20项通过。当前是持久意图日志，不是已激活的发送队列：未消费日志、未确认后清理、未衔接独立 SyncOutbox，不会自动重试这些日志；普通同步仍走旧路径。尚未覆盖 mem/句子档案/事件，不能宣称端到端离线恢复完成。下一步需要把日志转换、基线对账、请求冻结及回执清理接成一条链；原值不是远端基础版本，禁止据此猜 baseRev。

2026-09-12 回执接线（更新上段状态）：普通上传等待课程/进度保存结束，发送前读取当前账号日志，按实际 payload 内容/删除目标选取回执候选。服务器明确 ok=true 后，在 IDB 事务中按 key+scope+operationId 比较删除；在途新改动、失败回执、账号切换均不清理。准备阶段异常释放 in-flight，允许重试。Playwright 回归增到19项，新增实际 core 保存→模拟 API 成功/失败/晚到回执测试。仍未把独立 SyncOutbox 的不可变请求接入，刷新后不能自动消费/重放日志，baseRevs 基线对账也未完成；不能把本次回执清理误报为整个发送队列完成。

2026-09-12 迁移与恢复继续收口：`migrate-open-to-user.js` 要求显式 `--confirm`，将目标快照 token 固定后走账号级 `/api/sync/batch/resolve`，迁移备份可通过 `MIGRATION_BACKUP_DIR` 放到隔离目录；双实例实测 9 项通过，旧 `/api/import` 被测试代理拒绝且未被调用，重复迁移业务快照稳定。该回归已纳入 `npm test`；当前 Service Worker 为 `chunklab-58e020c0`。A/G1 仍未整体完成，完整业务权威读源、课程/进度/generation 同批、ack 原子性、会话升级与真机/生产恢复仍待处理。

2026-09-12 会话继续收口：持久 `chunklab.session.v1` 记录服务地址、token 和随机 epoch，API/账号存储/跨页监听共同校验；真实登录→退出→同账号再次登录回归通过，业务 owner 不因 epoch 变化而迁库。最新 Service Worker 为 `chunklab-5d6f0c2b`，`npm run test:accounts` 通过。

2026-09-12 回执边界继续收口：条件批次成功回执在同一 IDB 事务推进 accepted 基线、清除 pending，并按 operationId 清理匹配学习日志；失败或不匹配仍整体保留。真实批次回归 18 项通过；改动后需重新生成 SW 并跑最终回归，未部署。
# 2026-09-12 账号隔离收口补记

发布判定仍以 RELEASE_CHECKLIST.md 为准。新增 js/account-storage.js，四个页面的业务存储按服务地址+账号分区，切换后刷新，保留原账号待发记录。登录目标服务先验证后提交，失败保留原会话。

本轮证据：npm test、npm run test:accounts（浏览器10项）、sync-outbox（34项）、sync（22项）、stats-idb（20项）通过。旧版无归属数据保留在原键/原IDB，不自动上传，但确认归属/恢复入口未完成；真实认证完整流程、旧标签页升级和跨标签页ABA仍需验收。因此D1未关闭，不可发布，下一步继续这些固定缺口。
## 2026-09-12 继续执行：保存提交边界收口

- `js/idb.js` 支持在同一 readwrite 事务中提交句子档案、事件、学习同步日志和 `conditional-batch-v1.localGeneration`；同步元数据写失败会整体回滚。
- `core.js` 的 `saveAndNotify` 等待本地提交成功后才广播更新和调度云同步；IDB 失败时保留 localStorage 兜底但不上传未确认状态。
- 验证通过：`node e2e/sync-outbox.test.js` 36/0、`node e2e/batch-sync.test.js` 16/0、`node stats-idb.test.js` 28/0、`npm test`；SW 缓存已重生为 `chunklab-4dc58829`。
- 这只是 A 包的部分收口，业务小字段/课程进度同批、完整 GET 快照、旧页与 GET 期间编辑等仍未完成，不能标记上线通过。
