# 首版上线收口清单

日期：2026-09-12 建立；**2026-09-16 更新为「架构换血 + 内容 21.7×」候选版本**。范围：小批量内容、小范围试用；不承诺 8000 句全量首发。不部署、不清除用户数据、不引入框架重写。

此文件是发布判定入口。architecture-plan.md / PROJECT_PROGRESS.md 是设计和历史记录，不再从中无限追加发布任务。

## 放行规则

下面5道门全部通过才发布。自动测试通过不替代生产配置、真机和恢复演练。新增问题只有可复现的数据丢失/串号、安全暴露、核心流程阻断才进入本轮；其他问题入后续迭代。每个修复必须附回归证据，不以重构完成度作为门槛。

## 最新执行状态（2026-09-22）

以下状态优先于本文后面的一切记录（含 2026-09-16 段）。

**候选版本已生产部署**（2026-09-22 11:29，`DEPLOY_EXIT=0`，九步全绿）

- 候选 = `master@f5219ef`（本地 = 远端 = 线上）。**线上实测 == 本地 HEAD**：
  线上 `sw.js` 的 `CACHE = chunklab-5b6fbce9`、`core.js` = 206,608 B（只读探测，2026-09-22）。
- ⚠️ **本节更正下面 2026-09-16 表格里的「线上（2026-09-07 版）」「新内容从未上线」** —— 已失效。
  实测：`/content/manifest.json` **200 / 101,998 B**、`main.html` **308,909 B**、`/js/batch-sync.js` 200。

| 项 | 状态（2026-09-22 实测） | 证据 |
| --- | --- | --- |
| **F-001 静态路径绕过** | ✅ **已关闭** | 变体族 `//server/…`、`/%73erver/…`、`/%2Fserver/…`、`…%2Edb`、`…-wal` **全 403**（归属 nginx）；Express 侧 `server/index.js:353 app.use(createStaticGuard())` 已上线 |
| **新内容架构（66 deck）** | ✅ **已上线** | `/content/manifest.json` 200；`main.html` 308,909 B |
| **封面破图 / SW 装不上** | ✅ **已关闭** | 66/66 图 200 + `image/webp`；`lesson-placeholder.svg` 200；SW `registrations=1 / activated / 缓存 140 条`；封面破图 0 |
| **A1 统计提交竞态** | ✅ **已关闭**（非本轮新增） | 已在 `92e810d` 修好；`git log -S` 语句变迁 + 对照复现（OLD 丢 `[e3]` / HEAD 丢 `[]`）；护栏见 `e2e/stats-idb.test.js` §3b |
| **D-1/D-2 重复实现清理** | 🟡 已提交、**未部署** | 提交 `32b251f`（父 `f5219ef`）；123/123 本地绿；工作区 `CACHE=chunklab-6066d9f0` ≠ 线上 `5b6fbce9` |

**剩余阻塞仍为 4 条**（与 09-16 收敛结论一致，全部是生产环境 / 用户侧动作，无代码缺口）：

① **G2 残余** —— 生产环境「两个真实账号互不可读写」（需真实凭据；现有 25 项跑的是测试 token + 临时服务）
② **G3** —— 独立实例备份恢复演练（部署 `[3/9]` 只打全库快照，且服务端启动迁移**有损** ⇒ 回滚不能只回代码）
③ **G4** —— Android/iOS 真机验收
④ **G5** —— 发布授权（用户参与）+ 首发内容人工抽检 + **老用户 SW 升级实测**

**本轮新增登记（不阻塞，需拍板）**：

- ⚠️ **`best` 的 LWW 是条件触发的真缺陷**（2026-09-22 实测判定，非「既定语义」）：
  `best ∈ SYNC_KV_KEYS`（`core.js:107`）⇒ 走 LWW 整块替换；`mergeBest`（并集）**只在 rev 严格相等时**生效
  ⇒ 当 `remoteRev > localRev` 时，本地**未上行**的 `best[<deck>]` 条目被**静默丢弃**。
  证据：`.workbuddy/_audit/best-lww-defect.mjs`（用出厂真实纯函数，rRev>lRev 场景实测丢弃）。
  触发条件三条同时成立：本地有 `best` 记录 + 云端已有 `best`（rev≥1）+ **首次同步前未初始化 `revs.kv.best`**。
  ⚠️ `core.js:99-106` 自己把这种配置定性为「本机未上行的改动被他机覆盖的**根因**」，
  并已把 `mastered/reinforceBook/deletedItems` 移出 —— **`best` 未移出，属遗留同类问题**。
- **TOPO-1**（`server/data` 仍在静态根内）仍未消，现由 static-guard + nginx 双层挡着。

---

## （2026-09-16 段落，保留备查，已被上文取代）

以下为 2026-09-16 记录。⚠️ 其表格中「线上（2026-09-07 版）」「新内容从未上线」已被 2026-09-22 实测推翻。

**候选版本 = 架构换血 + 内容 21.7×**（本地/远程 `master` = `4ed4498`，2026-09-16 推送）

| 维度 | 线上（2026-09-07 版） | 本候选（2026-09-16） |
| --- | --- | --- |
| 内容加载 | 单文件全量（`oral8000.js`，**150 句**） | `content/manifest.json` + **按需分片** |
| 内容规模 | 150 句 | **66 deck（65 个 `oral-*`）/ 136 分片** |
| `main.html` | 221 KB / 10 个 script | **297 KB / 16 个 script** |
| SW 缓存 | `chunklab-3d0b14d6` | `chunklab-eb2369d3`（`HEAD` 值；工作区另有未提交改动，合并后须重跑 `gen-sw`） |

- **代码侧门禁**：`npm test` 在 `07d76e6` 上实测 **EXIT=0**（`check-sw` / `sw-hash` / `check-contrast` + 自证 / `sw-policy` / `deploy-safety` / 内容校验 / 31 个 e2e 专项 / 服务端套件）。
  其后 `4ed4498` 只新增一个**不在门禁链内**的 e2e 脚本，结论不变。
  ⚠️ 未复跑原因：工作区另有**并发进行的未提交改动**（`core.js`/`stats.html`/`sw.js`/`stats-consistency.test.js`，后者在门禁链内）会污染结果 —— 待其收工后再复跑。
- **发布前专项（2026-09-16 实测）**：`node e2e/release-check.js` → **24/0**（A 首屏字节 / B 66 deck 齐全 / C 分片按需 / D 375px 无溢出 / E 负向自证）；
  `node e2e/upgrade-check.js` → **24/0**（**本次新增**，见 G5）；`node e2e/e2e.js` → **149/0**；`node e2e/sw-cache.test.js` → **14/0**。
- **公网安全冒烟（2026-09-16 只读实测）**：`bash scripts/deploy-security-smoke.sh https://chunklab.jqka.top` → **48 通过 / 0 失败 / 1 WARN**（WARN 仅 `Server: nginx`）。
  33 个敏感路径全 **403**、未登录 `/api/data` 401、`/api/config` 报 `requireAuth=true`、无 `X-Powered-By`、有 HSTS、证书 2026-12-08 到期。
- ✅ **本轮（2026-09-17）实修一处 P0：新访客无限重载。**
  `core.js ensureCloud()` 的 `needAuth && (!isLoggedIn() || _loadGuest())` 与本次换血新引入的会话代次机制
  （`api.js` 的 `chunklab.session.v1` / `epoch` + `js/account-storage.js`）叠加成环：只要本地存有游客凭据，
  每次启动都静默重登 → `api.setToken` 写回令牌 → 代次变化 → `AccountStorage.changed()` 触发整页重载 → 再重登。
  **探针实测：新访客 6 秒 33 次导航，首屏永不落定。**
  - **归类：①类（本次改动新增的风险，必须清零）** —— 只读实测线上：`api.js` 无代次机制（`chunklab.session.v1` 命中 **0**）、
    `core.js` 不引用 `AccountStorage`（**0 次**）、`js/account-storage.js` **404** ⇒ 线上同一条件只多一次登录，**不重载**。
  - **已修**：`ensureCloud()` 改为仅 `!isLoggedIn()` 时才走静默游客引导。过期续期不受影响 ——
    令牌过期时 `api.js` 遇 401 会 `clearToken`，下次启动 `isLoggedIn()` 为假，仍进该分支用保存的凭据静默重登。
  - **已验证**：探针 **33 次 → 2 次**（第 2 次为游客创建后应有的一次），会话代次全程稳定；
    新增 `e2e/account-set-credentials.test.js` 13 项全过。
  - **为何此前 50+ 条 e2e 都没发现**：既有账号类 e2e 都先置 `chunklab_manual=1`（主动绕过游客路径）
    或用开放模式，**没有一条**覆盖「`requireAuth=true` 下的纯首次访客」。该路径现已纳入门禁。
- **除上述已修项外，自动代码侧未发现新的 P0/P1 阻断项。仍不可直接发布**，剩余为：
  ① **G2** 生产鉴权/HTTPS/限流与密钥配置（含两个真实账号互不可读写，需凭据）
  ② **G3** 独立实例备份恢复演练（需 SSH，且服务端启动迁移是**有损**的 → 回滚不能只回代码）
  ③ **G4** Android/iOS 真机验收
  ④ **G5** 发布授权（用户参与）
- ✅ **原第 ④ 条「D2 前端接入未接通」已于 2026-09-17 复核推翻并删除**（读 HEAD 提交版逐条核实，三句全不成立）：
  | 原断言 | 实测 | 证据 |
  | --- | --- | --- |
  | `core.js` 普通同步未发 `baseSeq`/`requestId` | ❌ 不成立 | `core.js:1567 putConditionalCloud()` 就是普通同步的**唯一**上行口（`1683` 行调用），内部 `BatchSync.stage(payload, state.baseline, expectedGeneration, operationReceipts)` = 基线 + 请求编号 + 代次，缺基线直接抛 `SYNC_BASELINE_REQUIRED` fail-closed |
  | `js/batch-sync.js` 未由 HTML 加载 | ❌ 不成立 | 四页全部加载：`main.html:1629` / `decks.html:475` / `stats.html:280` / `courses.html:193` |
  | 未进部署清单 | ❌ 不成立 | `scripts/deploy-prod.sh:34` 白名单已含；`sw.js:91` 预缓存已含；`scripts/check-deploy-files.js` 是专门防「清单漏 `js/`」的护栏 |
  → 与本文件 G1 表 `D2` 行「…**已接通**」及 `docs/implementation/2026-09-12-prelaunch-data-safety.md` 的收口记录一致 ⇒ **原条目属陈旧残留**（该文档第 73 行本就写明「后面的 D1/D2 历史段落不应再作为当前实现状态判断」）。
- **D1/D2 的剩余部分已并入 G2/G4**：D1 剩「生产实例验证鉴权/HTTPS/过期会话/旧页刷新」、D2 剩「生产环境确认所有公开写入口均启用条件保护」——两者都是**生产实例上的验证动作**，不是代码缺口，与 G2（生产配置）/G4（真机）本就是同一批动作，不再单列阻塞。
  → **剩余阻塞由 5 条收敛为 4 条**（G2 / G3 / G4 / G5）。
- ⚠️ **SW 缓存切换需用户硬刷一次**（CACHE 名变更 → `activate` 清旧缓存）。老用户升级路径的代码侧验收已由 `e2e/upgrade-check.js` 覆盖，**真机上的升级仍需随 G4 验**。
- ⚠️ **待单独确认（2026-09-16 发现，尚未定性）**：`best` 属 `SYNC_KV_KEYS`（LWW 整块替换语义）。
  实测**带云同步时 `best['<老deck>']` 会被清**，把 `**/api/**` 全 `abort` 则保留 ⇒ 出在「同步」这一路，**非本地迁移**。
  尚未判定是「LWW 的既定语义」还是「本地未上行记录被云端空值覆盖」的真缺陷（验证夹具只种 `localStorage`、未建模同步 revs，**无资格判定**）。
  **发布前值得单独查一次**；在 `e2e/upgrade-check.js` 中已明确降级为「打印观察」而非断言。

### 以下为 2026-09-12 及更早的执行记录（保留备查，已被上文取代）

- 本次收口审计补记：刷新后会从持久化的 `conditional-batch-v1.localGeneration` 恢复同步代次，避免旧页面从 0 重新开始导致后续条件写入永久冲突；连续快速保存会立即预留单调递增代次，所有保存都能完成且最新状态会继续同步。题库发布也复用持久化条件队列，服务端成功但回执丢失时可原样重试，不再产生发布状态分叉。批次真实页面回归 22/0，修订同步回归 53/0；旧页面收到 428 会显示刷新提示。
- 本次恢复链路补修：cloud-hold 仅阻断普通写入，允许受控的冲突比较、继续处理和双方备份读取；恢复页面提供“核对云端版本”，确认成功后自动解除暂停；重复恢复使用新的请求编号。账号隔离/恢复专项 25 项、`api.test.js`、`test:batch-sync` 和 `npm test` 均通过。跨平台 LF/CRLF 哈希回归与多标签页专项也已通过；最终 Service Worker 当前缓存版本为 `chunklab-8602257a`。
- 本次最终复跑：`npm test` 退出码 0；双设备同步 24/0、待发队列 36/0、统计 IDB 22/0、批次同步 22/0、8000 句移动模拟 18/0、账号隔离/登录回归通过；迁移专项改用系统临时端口后连续两次 9/0；部署清单覆盖检查通过。
- 部署护栏补强：`scripts/deploy-prod.sh` 现在要求显式传入目标主机，在远端快照前先执行 `npm test`、账号隔离、批次同步和 8000 句移动专项，并 fail-closed 校验远端 `NODE_ENV=production`、`REQUIRE_AUTH=true` 和不少于 32 字符的 `JWT_SECRET`；部署安全回归 11/11 通过。
- 公网安全冒烟补强：`scripts/deploy-security-smoke.sh` 现在直接验证未登录访问 `/api/data` 被拒绝，并确认 `/api/config` 报告 `requireAuth=true`；连接失败仍标为待人工判断，不会误报安全通过。
- 当前结论：自动代码侧未发现新的 P0/P1 阻断项，本轮不再扩大到低优先级重构。仍不可直接发布：生产鉴权/HTTPS/限流与密钥配置、独立实例备份恢复演练、Android/iOS 真机验收、旧页面升级后的关闭旧页操作和发布授权。完整统一权威事务、8000 句内容生产/智能合并继续延期。

- 2026-09-12 同步删除语义收口：事件墓碑现在在全量/增量响应都可下发；整账号选择的事件集合会明确删除云端多余事件，另一设备拉取后不会复活。修复旧 key→cid 迁移造成的学习日志回执无法匹配，以及在途批次阻塞后续拉取的问题。双设备同步专项 `node e2e/sync.test.js`：24/0。
- 本候选重新验证：`npm test` 退出码 0、服务端增量 65/0、待发队列 36/0、批次同步 19/0、统计 IDB 22/0、8000 句移动模拟 18/0；Service Worker 当前缓存版本为 `chunklab-b8968286`。
- 自动代码边界：8000 句分片/分页/增量保存与同步安全链路已具备回归证据；完整“所有业务字段同一事务”的进一步收敛和旧标签页强制升级属于发布前评估项，不再作为新增大改动。生产鉴权/HTTPS、隔离恢复演练、Android/iOS 真机和发布授权仍未完成，当前不可发布。

- 2026-09-12 最新候选：慢 IndexedDB 下统计写入已收敛为“当前提交 + 一个最新尾提交”，不同快照不会无限排队；8000 句移动模拟专项 18/0，含 40ms 写入延迟时队列最多 2 个提交，8000 条句档案与 200 条事件仍完整落盘。
- 最终自动回归：`npm test` 退出码 0；`npm run test:accounts`、`npm run test:batch-sync`、`node e2e/stats-idb.test.js`、`node e2e/mobile-8000.test.js` 均通过。Service Worker 已按最终代码重生成，当前缓存版本为 `chunklab-6c4b054b`。
- 2026-09-12 代次边界补强：课程/进度保存与 `conditional-batch-v1.localGeneration` 在同一 IDB 事务内推进；统一规范化服务地址作用域，兼容旧页面直接写 token 的场景。批次真实页面回归 19/0；最终 Service Worker 为 `chunklab-34bc9941`。
- 自动实现到此收口：8000 句的分片加载、IDB 增量保存、慢存储有界队列、条件同步和恢复/迁移护栏已有代码证据；这仍不是生产放行。G1 的完整业务权威事务边界、旧页面升级和完整事件替换语义，以及 G2—G5 的生产/备份/真机/发布授权仍需完成。
- 已完成并验证：生产条件写入护栏；题库发布/删除状态约束；整账号批次比较与“使用本机/使用云端”解决；双方归档与回执重试；恢复时待同步请求拦截；本地提交 Promise 等待入口。
- 自动回归通过：`npm test`、`npm run test:accounts`、`npm run test:batch-sync`、双设备同步 21 项、待发队列 36 项、统计迁移 21 项、8000 句移动模拟 17 项；Service Worker 和部署清单检查通过。
- 2026-09-12 继续执行：统计保存已移除每次答题的整份 stats 深拷贝；主页面/题库页导入入口等待本地提交确认后才刷新或跳转。专项 8000 句移动端模拟 15 项、IDB 21 项和 `npm test` 通过；这不替代 R1 原子业务提交、生产环境或真机验收。
- 2026-09-12 再次继续：学习记录、事件、同步日志与 `localGeneration` 已支持同一 IDB 事务提交；`saveAndNotify` 改为本地提交成功后才广播和调度云同步；新增真实 IndexedDB 原子提交/回滚回归，待发队列专项更新为 36 项，`npm test` 通过，SW 缓存已重生为 `chunklab-4dc58829`。A 包仍未整体完成。
- 2026-09-12 快照与代次收口：`/api/data` 与导出读取改为 SQLite 单事务快照，服务端返回的 `seq` 与数据来自同一读取时代；普通同步入队新增 `expectedGeneration` 校验，旧 payload 在本地代次变化时被拒绝并重建。下游回归 59 项、批次回归 17 项通过；最新 SW 为 `chunklab-5d6f0c2b`。
- 2026-09-12 D4 连续保存证据：8000 句移动模拟新增连续 100 次保存，17 项全通过；p50 12ms、p95 19ms、最大 21ms，最终8000条句档案与100条事件均可读回。仅为四倍CPU模拟，不替代真机。
- 2026-09-12 迁移/恢复回归：新增双实例开放模式→多用户模式迁移演练，9 项通过；缺少 `--confirm` 拒绝写入、代理拒绝旧 `/api/import`、重复迁移业务快照稳定。迁移备份支持独立目录，避免演练污染真实数据。
- 2026-09-12 提交边界补充：轻量 `business-mem-v1` 投影与句子档案/事件/学习意图/同步代次在同一 IDB 事务写入并覆盖回滚；该投影尚未成为完整业务权威读源，课程/进度及完整 ack 原子性仍是 G1 缺口。
- 2026-09-12 回执原子性补充：条件批次回执现在在同一 IDB 事务推进 accepted 基线、清除 pending，并按 operationId 清理匹配学习日志；真实浏览器专项 18 项通过。未匹配/失败回执仍保留原请求。
- 2026-09-12 会话收口补充：`chunklab.session.v1` 持久保存规范服务地址、token 和随机 epoch；API 读写、账号分区及跨页监听共同校验。真实登录→退出→同账号再登录回归通过，A→B→A 不复用旧 epoch；SW 已重生为 `chunklab-5d6f0c2b`。
- 2026-09-12 最终专项复跑：`npm test` 0 失败；批次 18/0、账号隔离/登录回归通过、统计 IDB 21/0、8000 句移动模拟 17/0。8000句连续100次保存 p50 12ms、p95 19ms、最大21ms；SW 当前为 `chunklab-58e020c0`。
- G1 代码侧已收口：普通保存、课程/进度、学习日志、事件删除、题库发布均已接入相应的持久化/条件保护；旧页面收到 428 会提示刷新，慢存储有界队列和丢回执重试已有回归证据。G1 仍需在真实生产实例完成配置与演练后才能放行。
- G2/G3/G4/G5 仍需真实环境、隔离恢复演练、Android/iOS 真机和用户授权；自动测试通过不等于可以上线。
- 本文件后面的 D1/D2 历史段落用于追踪过程，不应再作为当前实现状态判断。

## G1 数据安全：代码侧收口，待外部验收

| ID | 优先级 | 当前证据与缺口 | 完成条件 |
| --- | --- | --- | --- |
| D1 | P0 | 账号隔离、真实登录 A→B→A、服务地址隔离、本机恢复和恢复后受控云端核对已有专项证据；仍需生产实例验证鉴权、HTTPS、过期会话和旧页面刷新操作。 | 同一浏览器 A 退出→B 登录，B 不能读取/上传 A 数据；A 待发数据保留；服务地址切换同样验证。不允许以清空数据解决。 |
| D2 | P0 | 条件批量写入、持久请求编号/回执、课程/进度代次边界、冲突保留和题库发布持久重试已接通；生产环境仍需确认所有公开写入口均启用条件保护。 | 两设备同条记录/删除后重建/题库冲突不静默覆盖；可拒绝并提示，不要求智能合并。对外开放的写入路径不得绕过保护。 |
| D3 | P1 | 学习日志回放、回执清理、事件删除墓碑和刷新后重试已有回归；不支持的高阶智能合并仍明确延期。 | 普通答题、刷新、重试后队列可收敛；已确认记录不无限积累；删除不复活。不支持的操作明确阻止，不能悄悄忽略。 |
| D4 | P1 | IDB 增量写、有界尾队列、失败回滚/降级和 8000 句移动模拟已有回归；真实 Android/iOS 容量与耗时仍需验收。 | 连续答题/立即刷新/存储失败有回归；失败不显示保存成功或上传过期快照；待写快照有界。按首发真实容量测耗时和峰值。8000句首发才强制8000条热路径门槛。 |

实施顺序：D1 → D2 → D3 → D4 已完成代码侧收口，每包均有专项回归；不再重复扩展。自动测试不能替代 G2—G5 的真实配置、恢复演练、真机验收和发布授权。

### D1执行记录

2026-09-12：API请求绑定发送时的token、服务地址和本页面会话代次；响应头到达和响应体解析后均重新检查。旧账号401不清新账号token，旧数据响应不返回给调用方。同token退出再登录的本页往返也拒绝旧响应。api.test.js覆盖晚到401、跨标签页token变化、服务地址变化、响应体读取期间切换、正常401。

后续实现：四个页面接入AccountStorage，业务localStorage、IndexedDB及跨标签页通知按服务地址+uid分区；切换身份隐藏旧页面并重新加载，旧页面不能继续上传。登录先验证目标服务，成功后才更新凭据；失败不改当前会话，也不把原token发到目标登录服务。手动登录移除旧游客凭据，游客凭据绑定服务地址。

旧版本没有归属记录，不能猜测当前登录者就是所有者。现保留原始键和chunklab-idb、不自动挂给任何新账号、不自动上传。**这是隔离保护，不是迁移已完成；旧数据尚无可用的确认归属/恢复入口，本版不得直接发布。** 已有明确归属标记的数据库仍支持原迁移流程。

验证：npm test通过；npm run test:accounts通过（存储失败关闭访问、登录失败保护，以及10项真实浏览器存储隔离检查）；sync-outbox 34项、sync 22项、stats-idb 20项通过。账号隔离浏览器测试使用测试token和独立临时服务，证明本地读写/待发队列隔离，不等于真实鉴权登录全流程通过。stats-idb夹具显式标记数据所属账号，未知归属保留由account-isolation单独验证。

本轮续作：设置→数据备份新增“取回旧版数据”，勾选归属确认后仅下载备份。读取原LS与原IDB，保留原始存档和队列，不写当前账号、不更改归属、不删除原件、不上传。读取失败可重试。此入口替代上文“无可用入口”的历史状态，但**不代表安全合并恢复完成**，不能直接把已有覆盖式导入视为解决方案。

验证更新：npm test通过；test:accounts通过，隔离/旧数据取回13项，并新增真实鉴权登录→退出→另一账号→返回原账号测试和实际下载文件检查。修复主动退出后自动创建游客的问题。取回前未勾选确认会被阻止，读取失败原数据不变，下载内容可解析且当前账号数据未覆盖。

会话代次更新：`api.js` 新增持久 `chunklab.session.v1`（规范 base、token、随机 epoch），每次登录、退出、续期或服务切换更新；`AccountStorage` 启动和跨标签页事件同时校验 epoch，API 仍在响应头与响应体解析后校验。旧页面只写兼容镜像时，新页面会放弃不一致的旧记录并使请求失效。验证：`api.test.js`、`test:accounts`（含真实登录→退出→同账号再次登录）通过；业务 owner 仍只由服务地址+uid决定，不因 epoch 变化迁库。

### 本机恢复续作（最新状态）

已提供预览→确认恢复到本机。只合并不冲突内容；相同编号不同值、同名重复、已删除记录及旧待同步操作会阻止整次恢复，不自动选一方覆盖。保留当前设置。预览后当前存储发生变化也拒绝提交。

恢复使用Web Locks串行协调，先持久化恢复锁、冻结业务写入和在途响应处理，再写新命名空间/新数据库及备份，最后单键切换活动版本。原数据库、原始旧数据及前一版本均保留。失败或刷新后保持冻结，可取消未完成操作再预览重试；取消只清理本次未激活的副本，不删除原数据。暂不支持Web Locks的浏览器明确拒绝恢复。

证据：test:accounts通过（隔离/取回/恢复共23项及真实登录、实际UI下载与恢复）；测试包含副本写失败、小字段配额失败、刷新后冻结、取消清理、旧预览拒绝及成功重试。npm test通过；sync-outbox 34项、stats-idb 20项通过。未据此宣称所有断电时刻、真机或旧标签页都已覆盖。

**恢复后仅本机使用，云端同步持久暂停，尚无解除入口。** 防止绕过D2未完成的写入保护；这是明确剩余工作，不是云端恢复完成。D1仍未通过：剩余为恢复后云同步的安全重新接入（依赖D2）、登录过期续期、旧版本标签页升级及跨标签页同token往返ABA。旧“导入备份”覆盖式入口也未由本次新恢复流程替换，不应作为安全合并的替代路径。后续不扩展新的功能包。

### D2服务端条件批量写入（2026-09-12续作，尚未接通前端）

PUT /api/data 新增可选 baseSeq（客户端已读取的账号云端版本），在同一数据库事务内检查版本、写全部实体并推进版本。版本不同返回409，包含batch冲突原因；不抬高版本强制覆盖。成功返回提交版本seq。该策略会拒绝互不相干的并发修改，首发先偏保守，不冒充逐行智能合并。

可选requestId必须搭配baseSeq。成功回执与业务数据同事务保存；相同账号/编号/内容重试返回原seq，不重写；编号复用为不同内容返回409。每账号保留最近256条回执，过期回执的原请求仍因旧baseSeq拒绝。客户端不能将原回执seq当作“服务器目前最新版本”，也不能给旧载荷替换为新baseSeq再重试。断线后须保留原请求，不能重新生成编号。

server/batch-version.test.js 已加入npm test：26项验证，包括并发竞争、删除后旧写复活拦截、事务中途失败与回执一同回滚、重试、账号隔离和回执淘汰。npm test通过（全套运行时该专项23项，随后新增容量/账号编号3项并单独26项通过）。测试使用独立临时数据库。

**D2仍未通过**：core.js普通同步尚未发送baseSeq/requestId；现有不带baseSeq的兼容写入、课程直写、导入等入口尚未强制条件版本；batch冲突尚无前端比较/恢复流程。下一步必须将“已读版本+固定请求+持久回执”接入前端并封堵旁路，而不是只加字段或在提交前临时读取最新seq。恢复后云同步禁用状态保持不变。

### D2前端固定请求队列（尚未启用普通同步）

新增js/batch-sync.js：基线与固定请求保存在账号业务IDB的syncIntents中，落盘后才允许发送；observe要求旧基线匹配，stage要求调用方提供此前已知基线，不代替调用方临时读取云端；原请求未确认时阻止下一批和基线推进。重试保持requestId/baseSeq/内容，确认回执与清理队列在同一事务内完成；失败或冲突保留原请求。Web Locks协调多页面。回执seq只代表该提交，不代表云端最新版本。

npm run test:batch-sync通过：服务端26项、前端真实浏览器12项，含真实服务端成功但网络回执丢失后刷新重试、存储失败、基线变化、两页面竞争和冲突保留。测试使用独立临时账号与数据库。此模块尚未由HTML加载、未进入部署清单，也尚未替换core.js普通同步，避免把组件专项通过冒充产品接入完成。

下一步固定为core.js接入：先建立已接受云端快照与本地修改的对应关系；启动时先恢复原请求，不能先拉取合并新基线；后续新修改只能确认其来源后提交。同步冲突展示及旧写入入口强制保护仍在D2内。恢复后的自动上云仍禁用。

## G2 生产安全配置：待环境验证


- 实际实例启用生产模式、鉴权、独立密钥和HTTPS。
- 未登录拒绝访问，两个真实测试账号互相不可读写。
- 静态文件白名单、限流和敏感数据访问检查。
- 代码有相关护栏，不代表部署已经符合；不打印密钥。

## G3 备份与回退：待环境演练

- 自动备份执行与保留策略确认，失败可见。
- 从备份恢复到独立目录/实例，核对课程和学习记录。
- 明确发布失败后的代码/数据库恢复步骤；不在真实用户库上练习。

## G4 手机核心流程：待真机验收

- Android与iOS各完成：进入→答题→刷新→复习→导入→统计。
- 验证弱网/断网重连、返回前台、可点击区域、无遮挡和横向溢出。
- 首发内容人工抽检。当前393px/CPU减速模拟仅辅助，不替代真机。

## G5 固定版本发布：待G1—G4完成

- 整理当前改动形成可识别的候选版本；重新运行测试。
  → 2026-09-16：候选 = `master@4ed4498`；`npm test` 在 `07d76e6` 上 EXIT=0（见上表进度与未复跑说明）。
- 验证老版本升级、IDB数据保留与Service Worker更新。
  → 2026-09-16 **代码侧已补专项** `node e2e/upgrade-check.js`（**24/0**），覆盖：三代旧档案 key
  （`builtin-daily#cid` / `daily-*#cid` / `daily-*#整句原文`）迁移到**真实存在**的新 `oral-*` deck、值保留、幂等、
  不误伤（非白名单 deck / 孤儿 cid 原样保留）、句子级 key 守恒、SW 旧缓存被清、负向自证。
  → **仍未验**：真机上的实际升级 + IDB 数据保留 + SW 切换（需随 G4 真机一并验）。
- 小范围试用；用户确认后再部署生产。

## 自动证据

- npm test：前后端单测、部署护栏、内容校验、后端备份工具回归。
- `node e2e/release-check.js`（`npm run e2e:release`）：**新内容架构验收** —— 首屏字节（CDP 权威）/ manifest deck 齐全 / 分片按需 / 375px 无横向溢出，含负向自证。
- `node e2e/upgrade-check.js`（`npm run e2e:upgrade`）：**老版本升级验收** —— 三代旧档案 key → 新 `oral-*` deck / 值保留 / 幂等 / 不误伤 + 守恒 / SW 旧缓存清除，含负向自证。
- node e2e/sync-outbox.test.js：课程恢复、日志事务、学习删除等专项；不是全数据并发验收。
- node e2e/sync.test.js：既有双设备及冲突处理。
- node e2e/stats-idb.test.js：迁移/刷新/增量持久化。
- node e2e/mobile-8000.test.js：模拟内容加载/分页；不包含真实手机连续答题保存峰值。

## 明确延后

完整8000句内容生产、框架迁移、微服务、全面拆文件、智能冲突自动合并、完整冲突历史UI、增量下行全面接入、AI恢复、非阻断视觉打磨。

用户参与：首发内容确认、部署条件提供、真机体验反馈及最终发布授权。代码和自动验收由本任务推进。
