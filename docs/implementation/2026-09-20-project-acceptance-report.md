# 项目实施状态核查报告

- Updated: 2026-09-20
- Worktree: `D:/06-project/chunk-practice`, branch `master`
- Base commit: `a88c690cf588d951c98d9d812ea1be907735adca`
- Method: 先只读核对实施文档、代码入口和测试清单，再用隔离浏览器、严格零重试全量和发布专项完成验证；本报告仍不把本轮收尾等同于全部历史计划或生产发布。

## 总体结论

上一轮收尾验收有 100/100 的通过记录，但 review 发现同步间歇失败、跨平台进程清理、手机完整流程和版本指纹仍缺证据。本轮已补齐这些证据：Windows 严格零重试全量 101/101，WSL Ubuntu unit 34/34、checks 16/16（隔离副本重建 Linux 原生依赖）。

## 历史计划状态

| 计划 | 文档状态 | 已确认内容 | 未完成或未证实内容 |
|---|---|---|---|
| 首版上线数据安全收口 | `code-side-complete; external-release-gates-pending` | 仍保留为外部发布门槛待完成 | 会话/IDB 原子提交、普通同步接入、恢复闭环、队列收敛、发布演练等大量条目未勾选；不能当作发布完成 |
| 启动竞态与统计口径 | `complete` | 首页本地优先、刷新不误入课程、进度口径相关实现及本轮桌面/手机连续流程复验 | 本轮不再追加该计划范围外的入口重构 |
| 统一课程领域模型 | `complete` | 课程目录与入口重构已有验收记录 | 本轮只核对主流程，不重新扩展其范围 |
| 新版课程包导入 | `complete` | 1.1/2.0 契约、导入、输入/意群能力和真实 e2e 均通过；严格全量 101/101 | 外部 course-creator 导出互操作仍需独立样本，不作为本计划当前消费者验收的阻断 |
| 首页生命周期简化 | `complete` | entry/lifecycle、首页首屏、入口消费和动态加载已有验收记录 | 本轮复验刷新、继续、完成和手机布局 |
| 工程维护改造 | `in-progress` | CI、build/check、lint/typecheck、纯领域模块、测试清单、桌面/手机页面和生成物复核部分完成 | core runtime、首页实际源码接入、服务端装配等历史拆分条目仍未完成 |
| 同步恢复可靠性 | `complete` | 诊断、自动恢复入口、容量分类、专项浏览器测试和严格 100/100 回归完成 | 本轮不扩大同步协议改造；项目级协议、事件统计重建仍独立规划 |
| 同步恢复验收 | `complete` | 跨标签、丢回执刷新、局部落盘失败、账号切换、容量阻塞有直接证据；严格全量 100/100 | `run-tests.cjs` 实际串行；本轮结论是严格串行通过，不代表并行稳定性 |

## 代码层面已确认的问题

`main.html` 的 `buildFallbackExplanation` 已修正：只有真实含义字段才显示“核心含义”；只有译文时不复制到讲解；没有场景不生成课程类别模板，也不把原题自动充当新增例句。结构化讲解、已有字段兼容和 HTML 转义均保留；内容质量仍需后续逐句补齐，未扩成批量自动编写。

`scripts/run-tests.cjs` 仍按清单串行执行；默认只有 browser 组允许重试一次，但现在每次运行有独立 runId、每次 attempt 即时落盘、完整 stdout/stderr、退出码、耗时和超时标记，并记录 manifest 与脏工作区内容指纹。`scripts/run-tests.test.js` 已验证重试、spawn error、敏感字段脱敏和父子进程树清理；严格零重试全量已通过。

`e2e/exit-clears-choices.test.js` 已补上 health 就绪等待、服务输出诊断、退出等待和临时目录生命周期；专项及严格全量均通过。

## 本轮验收范围

1. 用隔离小课程复验首页刷新、选课、答题、详解、退出、继续、完成课节、下一未完成课节、复习和统计返回；覆盖桌面与 390px 视口。
2. 修正讲解兜底：只有真实含义字段显示“核心含义”；没有场景不生成类别模板；没有有效讲解显示明确空态；不重复原题；保留 HTML 转义和结构化字段。
3. 改进测试结果留痕和服务生命周期诊断，严格关闭重试运行一次全量，若失败保留为未完成并记录根因。
4. 执行构建、内容生成物、SW、部署清单和发布专项检查；外部 CI、真实设备、生产部署仍按条件记录。

## 本轮结果

- `node e2e/learning-journey.test.js`：完成已学完课节后进入下一未完成课节。
- `node e2e/content-explanation-refresh.test.js`：空态、普通句、结构化解释和恶意文本转义通过。
- `node e2e/mobile-learning-explanation.test.js`：390×844 完成答题、长讲解面板内部滚动、顶部/底部截图、关闭和下一题，无横向溢出。
- `node e2e/mobile-8000.test.js`：393×852，无横向溢出；8000 句热保存 p50 16ms、p95 21ms、max 23ms。
- `npm run build:check`、`npm run content:check-generated`、SW 严格检查、部署清单检查、`git diff --check`：通过。
- `npm run e2e:release`：24/24；首屏 265.4 KB，1/136 分片，375px 无横向溢出。
- `TEST_RETRIES=0 npm test`：runId=`20260920062725593-18824`，101/101，0 flaky，工作区前后内容指纹一致；WSL Ubuntu 隔离 Linux 依赖副本 `unit`：34/34，`checks`：16/16。

## 证据边界

- 严格结果保存在 `output/test-results/20260920062725593-18824/summary.json`；101/101，所有项目首次通过，无 flaky，工作区前后指纹一致。
- 当前工作区含大量用户既有改动和未跟踪功能；本报告不判断这些改动是否应提交，也不执行回退。
- 本轮完成后，未勾选的历史计划仍保持原状态；需要单独规划的项目级同步协议、事件统计重建和实体版本迁移不在本轮实现。真实设备、外部 course-creator 互操作、生产部署与发布演练也未执行。
